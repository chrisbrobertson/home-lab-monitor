port: 9100

services:
  - name: "SSH"
    type: port
    port: 22

  - name: "Home Lab Monitor"
    type: port
    port: 8888

babysit:
  scan_paths:
    - "~/sisyphus-logs"
  include_last_action: true
